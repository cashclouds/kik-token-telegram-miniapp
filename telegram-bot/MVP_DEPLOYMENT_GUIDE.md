# 🚀 KIK Collectibles Bot - MVP Deployment Guide

## ✅ MVP Готово к развертыванию!

Все критические функции реализованы и готовы к запуску.

---

## 📋 Что реализовано

### ✅ Инфраструктура
- [x] **PostgreSQL интеграция** - Автоматическое переключение между in-memory (MVP) и PostgreSQL
- [x] **Vercel конфигурация** - Оптимизирована для serverless функций
- [x] **Зависимости установлены** - openai, pg, telegraf, cloudinary, redis и др.

### ✅ База данных
- [x] **database-schema.sql** - Полная SQL схема для PostgreSQL
- [x] **Методы в db.js**:
  - createToken() - Создание токенов
  - getUserTokens() - Получение токенов пользователя
  - attachPictureToToken() - Привязка картинок
  - savePicture() - Сохранение метаданных картинок
  - getUserStats() - Статистика пользователя
  - updateUserStats() - Обновление статистики
  - recordDailyActivity() - Запись ежедневной активности
  - getYesterdayActivity() - Проверка вчерашней активности

### ✅ OpenAI интеграция
- [x] **OpenAI SDK** добавлен в pictureService.js
- [x] **DALL-E 3** интеграция для генерации изображений
- [x] **Умный fallback** - Автоматический переход на мок при отсутствии API key
- [x] **Обработка ошибок** - Graceful degradation

### ✅ Сервисы
- [x] Picture Tokens система
- [x] Реферальная система
- [x] Система наград
- [x] Мультиязычность (27 языков)

---

## 🔧 Быстрый старт (5 минут)

### 1️⃣ Настройка переменных окружения

Создайте `.env` файл в папке `telegram-bot/`:

```bash
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_from_@BotFather

# OpenAI (опционально для AI генерации)
OPENAI_API_KEY=sk-proj-...your_openai_api_key

# PostgreSQL (опционально, без него работает in-memory)
DATABASE_URL=postgresql://user:password@host:5432/database

# Cloudinary (опционально для загрузки фото)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Redis (опционально для кэширования)
REDIS_URL=redis://localhost:6379

# Vercel (автоматически устанавливается при деплое)
VERCEL_URL=auto
NODE_ENV=production
```

### 2️⃣ Развертывание на Vercel

```bash
# Перейдите в папку telegram-bot
cd telegram-bot

# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Залогиньтесь в Vercel
vercel login

# Разверните проект
vercel --prod
```

**После деплоя:**
1. Скопируйте URL вашего Vercel приложения (например: `https://your-app.vercel.app`)
2. Установите webhook для Telegram бота:
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-app.vercel.app/api/webhook"
```

### 3️⃣ Настройка PostgreSQL (опционально)

Если хотите использовать реальную базу данных вместо in-memory:

**Вариант A: Vercel Postgres**
```bash
vercel postgres create
vercel env add DATABASE_URL
```

**Вариант B: Другой провайдер (Supabase, Neon, Railway)**
1. Создайте PostgreSQL базу данных
2. Импортируйте схему: `psql <DATABASE_URL> < database-schema.sql`
3. Добавьте DATABASE_URL в переменные окружения Vercel

---

## 🎯 Проверка работы

### Тест 1: Проверка health endpoint
```bash
curl https://your-app.vercel.app/api/health
```
Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-12-13T15:00:00.000Z",
  "database": "in-memory" или "postgresql"
}
```

### Тест 2: Проверка webhook
```bash
curl https://your-app.vercel.app/api/webhook
```

### Тест 3: Проверка бота в Telegram
1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением

---

## 📦 Структура проекта

```
telegram-bot/
├── api/                    # Vercel Serverless Functions
│   ├── webhook.js         # Основной webhook endpoint
│   ├── health.js          # Health check
│   ├── claim.js           # API для claim токенов
│   ├── upload-photo.js    # Загрузка фото
│   └── user/[userId].js   # Данные пользователя
├── src/
│   ├── commands/          # Telegram команды
│   ├── database/
│   │   └── db.js          # ✅ Обновлено: PostgreSQL + новые методы
│   ├── services/          # Бизнес-логика
│   │   └── pictureService.js  # ✅ Обновлено: OpenAI DALL-E
│   ├── utils/             # Утилиты
│   └── locales/           # 27 языков
├── database-schema.sql    # ✅ НОВОЕ: SQL схема
├── vercel.json            # ✅ ИСПРАВЛЕНО: Конфигурация Vercel
├── package.json           # ✅ Все зависимости установлены
└── .env.example           # Пример переменных окружения
```

---

## 🔑 Необходимые API ключи

### Обязательные (для MVP)
- ✅ **BOT_TOKEN** - Получите от [@BotFather](https://t.me/BotFather)

### Опциональные (для полного функционала)
- ⚡ **OPENAI_API_KEY** - Для AI генерации изображений ([platform.openai.com](https://platform.openai.com))
- ⚡ **DATABASE_URL** - Для постоянного хранения данных
- ⚡ **CLOUDINARY_*** - Для загрузки и хранения фото
- ⚡ **REDIS_URL** - Для кэширования

---

## 🚨 Важные моменты

### MVP Mode (без DATABASE_URL)
- ✅ Бот работает с in-memory хранилищем
- ⚠️ Данные сбрасываются при рестарте
- ✅ Идеально для тестирования
- ✅ Не требует настройки базы данных

### Production Mode (с DATABASE_URL)
- ✅ Данные сохраняются постоянно
- ✅ Поддержка множества пользователей
- ✅ Масштабируемость
- ⚠️ Требует настройки PostgreSQL

### OpenAI Mode
- С API key: Реальная генерация изображений через DALL-E 3
- Без API key: Fallback на placeholder изображения

---

## 📊 Мониторинг

### Vercel Dashboard
```
https://vercel.com/dashboard
```
- Просмотр логов
- Метрики производительности
- Использование ресурсов

### Логи в реальном времени
```bash
vercel logs --follow
```

---

## 🐛 Troubleshooting

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте webhook: `curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo`
2. Проверьте логи: `vercel logs`
3. Проверьте переменные окружения в Vercel Dashboard

### Проблема: Database errors
**Решение:**
1. Если DATABASE_URL не установлен - это нормально (использует in-memory)
2. Если установлен - проверьте корректность URL
3. Проверьте, что схема импортирована: `psql <URL> -c "\dt"`

### Проблема: OpenAI generation fails
**Решение:**
1. Проверьте OPENAI_API_KEY в Vercel
2. Проверьте баланс аккаунта OpenAI
3. Без API key бот использует placeholder изображения

---

## 🎉 Готово к запуску!

Все критические функции реализованы:
- ✅ PostgreSQL интеграция с умным fallback
- ✅ OpenAI DALL-E для генерации изображений
- ✅ Picture Tokens система
- ✅ Реферальная система
- ✅ Система наград и уровней
- ✅ Мультиязычность (27 языков)
- ✅ API endpoints для Mini App
- ✅ Vercel serverless deployment

## 📞 Следующие шаги

1. **Тестирование MVP**
   - Протестируйте основные функции
   - Проверьте работу на мобильных устройствах
   
2. **Настройка PostgreSQL**
   - Для production рекомендуется настроить постоянную БД
   
3. **Добавление OpenAI API key**
   - Для реальной генерации изображений
   
4. **Мониторинг и аналитика**
   - Настройте мониторинг в Vercel
   - Анализируйте логи и метрики

---

**Создано:** 13.12.2025
**Статус:** ✅ MVP Ready
**Версия:** 1.0.0
