# 📚 Полное объяснение структуры проекта KIK Collectibles

## 🎯 Что это за проект?

**KIK Collectibles** - это Telegram Mini App для коллекционирования NFT картинок с токенами. Проект состоит из **ДВУХ отдельных приложений**, которые работают вместе.

---

## 1️⃣ СТРУКТУРА ПРОЕКТА

### 📁 Общая структура

```
C:\Users\User\OneDrive\Документы\token\files-v2\
│
├── 📂 telegram-bot/          ← BACKEND + Mini App (Telegram Bot)
│   ├── api/                  ← Serverless API endpoints для Vercel
│   ├── public/               ← Статические файлы Mini App
│   ├── src/                  ← Логика бота и сервисы
│   └── vercel.json           ← Конфигурация для Vercel
│
├── 📂 wallet-app/            ← FRONTEND (Web3 Wallet приложение)
│   ├── app/                  ← Next.js приложение
│   ├── components/           ← React компоненты
│   ├── lib/                  ← Утилиты и контракты
│   └── vercel.json           ← Конфигурация для Vercel
│
├── 📂 contracts/             ← Smart контракты Solidity
├── 📂 scripts/               ← Скрипты для деплоя контрактов
└── 📄 hardhat.config.js      ← Конфиг Hardhat для блокчейна
```

---

## 2️⃣ ДЕТАЛЬНОЕ ОПИСАНИЕ КОМПОНЕНТОВ

### 🤖 telegram-bot/ - Backend + Telegram Mini App

Это **ГЛАВНЫЙ ПРОЕКТ** для Telegram бота с NFT коллекцией.

#### Структура telegram-bot/:

```
telegram-bot/
│
├── 📂 api/                          ← Serverless функции для Vercel
│   ├── webhook.js                   ← Обработчик сообщений от Telegram
│   ├── health.js                    ← Проверка работы сервера
│   ├── claim.js                     ← API: получить ежедневные токены
│   └── user/
│       └── [userId].js              ← API: получить данные пользователя
│
├── 📂 public/                       ← Mini App (HTML/CSS/JS)
│   ├── index.html                   ← Главная страница Mini App
│   ├── css/
│   │   └── app.css                  ← Стили Mini App
│   └── js/
│       ├── app.js                   ← Логика Mini App
│       └── i18n.js                  ← Мультиязычность
│
├── 📂 src/                          ← Основная логика бота
│   ├── index.js                     ← Точка входа бота (НЕ используется на Vercel)
│   ├── commands/
│   │   └── pictureTokens.js        ← Команды бота (/start, /daily, /app)
│   ├── services/
│   │   ├── tokenService.js         ← Управление токенами
│   │   ├── pictureService.js       ← Управление картинками NFT
│   │   ├── referralService.js      ← Реферальная система
│   │   └── nftService.js           ← Интеграция с блокчейном
│   ├── database/
│   │   └── db.js                   ← In-memory база данных
│   ├── locales/
│   │   ├── en.json                 ← Переводы (28 языков)
│   │   ├── ru.json
│   │   └── ...
│   └── utils/
│       ├── logger.js               ← Логирование
│       └── redis.js                ← Кэширование (mock)
│
├── 📄 vercel.json                   ← Конфигурация Vercel
├── 📄 package.json                  ← Зависимости Node.js
└── 📄 .env.production.example       ← Пример переменных окружения
```

#### Что это делает?

1. **Telegram Bot** - отвечает на команды пользователей
2. **Mini App** - веб-приложение, которое открывается внутри Telegram
3. **API** - обрабатывает запросы от Mini App (claim токенов, получение данных)

---

### 💰 wallet-app/ - Web3 Wallet приложение (Next.js)

Это **ОТДЕЛЬНОЕ ПРИЛОЖЕНИЕ** для работы с криптокошельками и блокчейном.

#### Структура wallet-app/:

```
wallet-app/
│
├── 📂 app/                          ← Next.js 14 App Router
│   ├── layout.tsx                   ← Основной layout
│   ├── page.tsx                     ← Главная страница
│   └── globals.css                  ← Глобальные стили
│
├── 📂 components/                   ← React компоненты
│   ├── WalletDashboard.tsx         ← Главная панель кошелька
│   ├── SimpleWallet.tsx            ← Простой кошелек
│   ├── TokenTransfer.tsx           ← Перевод токенов
│   ├── TokenRental.tsx             ← Аренда токенов
│   ├── UserLevel.tsx               ← Уровень пользователя
│   ├── FriendsList.tsx             ← Список друзей
│   └── Web3Provider.tsx            ← Провайдер Web3 (RainbowKit)
│
├── 📂 lib/                          ← Утилиты
│   ├── contracts.ts                ← Конфигурация контрактов
│   └── kikToken.ts                 ← Взаимодействие с KIK Token
│
├── 📄 vercel.json                   ← Конфигурация Vercel
├── 📄 package.json                  ← Зависимости
├── 📄 .env.local                    ← Локальные переменные
└── 📄 .env.production.example       ← Пример для продакшена
```

#### Что это делает?

1. **Web3 Wallet** - подключение MetaMask, WalletConnect
2. **Token Management** - просмотр баланса KIK токенов
3. **Transfers** - перевод токенов другим пользователям
4. **Rental** - аренда токенов на время

---

### 📜 contracts/ - Smart контракты

```
contracts/
├── KIKToken.sol              ← Основной токен ERC-20
├── KIKTokenV4.sol            ← Последняя версия с функциями
├── CollectiblesNFT.sol       ← NFT коллекция
├── Marketplace.sol           ← Маркетплейс для NFT
├── ReferralSystem.sol        ← Реферальная система
└── RewardDistributor.sol     ← Распределение наград
```

**Развернут на:** Polygon Amoy Testnet (Chain ID: 80002)  
**Адрес KIK Token:** `0x636C84f54cE96dfb4AE8B0D7c1420170bF8c22b7`

---

## 3️⃣ АРХИТЕКТУРА: КАК ВСЁ РАБОТАЕТ ВМЕСТЕ

### 🎨 Схема взаимодействия

```
┌─────────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛЬ                              │
│                    (Telegram App)                            │
└─────────────────────────────────────────────────────────────┘
                           │
                           ├──────────────┬──────────────┐
                           ▼              ▼              ▼
                    ┌─────────────┐  ┌──────────┐  ┌──────────┐
                    │ Telegram Bot│  │ Mini App │  │  Wallet  │
                    │   Commands  │  │  (HTML)  │  │   App    │
                    └─────────────┘  └──────────┘  └──────────┘
                           │              │              │
                           ▼              ▼              ▼
                    ┌─────────────────────────────────────────┐
                    │         VERCEL DEPLOYMENT                │
                    ├─────────────────┬───────────────────────┤
                    │  telegram-bot/  │    wallet-app/        │
                    │  (Serverless)   │    (Next.js)          │
                    └─────────────────┴───────────────────────┘
                           │              │
                           └──────┬───────┘
                                  ▼
                    ┌─────────────────────────────┐
                    │  POLYGON AMOY TESTNET       │
                    │  Smart Contracts            │
                    │  - KIK Token                │
                    │  - Collectibles NFT         │
                    └─────────────────────────────┘
```

### 🔄 Поток работы пользователя

#### Сценарий 1: Telegram Bot + Mini App (telegram-bot/)

1. **Пользователь открывает бота в Telegram**
   ```
   Пользователь → @YourBot → /start
   ```

2. **Бот отвечает приветствием**
   ```
   Бот → "Добро пожаловать! Используй /app для открытия Mini App"
   ```

3. **Пользователь нажимает /app**
   ```
   Бот → [Кнопка "🚀 Open App"] (Web App Button)
   ```

4. **Открывается Mini App внутри Telegram**
   ```
   URL: https://your-bot.vercel.app/
   Загружается: telegram-bot/public/index.html
   ```

5. **Mini App показывает:**
   - 💎 Баланс токенов (например: 150 KIK Tokens)
   - 🎨 NFT коллекцию (картинки, прикрепленные к токенам)
   - 🎁 Кнопку "Claim Daily" для получения новых токенов
   - 🏆 Leaderboard (топ пользователей)
   - 👥 Друзья (рефералы)

6. **При клике на "Claim Daily":**
   ```
   Mini App → POST /api/claim → Serverless функция
   → Выдаются новые токены
   → Mini App обновляет баланс
   ```

#### Сценарий 2: Web3 Wallet App (wallet-app/)

1. **Пользователь открывает wallet-app URL**
   ```
   Browser → https://your-wallet.vercel.app
   ```

2. **Видит страницу подключения кошелька**
   ```
   [Connect Wallet] - кнопка для MetaMask/WalletConnect
   ```

3. **Подключает MetaMask**
   ```
   MetaMask → Polygon Amoy Network
   → Показывает баланс KIK Tokens
   ```

4. **Доступные действия:**
   - 💸 Transfer - перевести токены другу
   - 🔄 Rental - арендовать токены
   - 📊 Dashboard - просмотр статистики
   - 👥 Friends - список друзей

---

## 4️⃣ DEPLOYMENT НА VERCEL

### ⚠️ ВАЖНО: ДВА ОТДЕЛЬНЫХ ПРОЕКТА

**telegram-bot/** и **wallet-app/** - это **ДВА НЕЗАВИСИМЫХ ПРИЛОЖЕНИЯ**, которые нужно деплоить ОТДЕЛЬНО.

### Вариант 1: Один GitHub репозиторий, два Vercel проекта ✅ (РЕКОМЕНДУЕТСЯ)

**Текущая структура:** Один репозиторий на GitHub
```
https://github.com/cashclouds/kik-token.git
├── telegram-bot/
└── wallet-app/
```

**На Vercel:** Создаем **ДВА проекта**

#### Проект 1: Telegram Bot

```
Project Name: kik-collectibles-bot
Root Directory: telegram-bot
Framework: Other
Deploy URL: https://kik-collectibles-bot.vercel.app
```

**Переменные окружения:**
- `BOT_TOKEN` - токен от @BotFather
- `WEBHOOK_SECRET` - случайная строка
- `CONTRACT_ADDRESS` - адрес KIK Token
- `NODE_ENV=production`

#### Проект 2: Wallet App

```
Project Name: kik-wallet-app
Root Directory: wallet-app
Framework: Next.js
Deploy URL: https://kik-wallet-app.vercel.app
```

**Переменные окружения:**
- `NEXT_PUBLIC_KIKTOKEN_ADDRESS` - адрес контракта
- `NEXT_PUBLIC_AMOY_RPC_URL` - RPC Polygon
- `NEXT_PUBLIC_CHAIN_ID=80002`

### Вариант 2: Два GitHub репозитория (альтернатива)

Можно разделить на два репозитория, но это не обязательно.

---

## 5️⃣ КАК ЭТО РАБОТАЕТ ДЛЯ ПОЛЬЗОВАТЕЛЯ

### 🎮 User Journey - Telegram Bot (основной функционал)

```
📱 Пользователь → Открывает Telegram
    │
    └─→ Находит бота @YourBot
        │
        ├─→ Отправляет /start
        │   └─→ Бот: "Добро пожаловать в KIK Collectibles!"
        │       └─→ Показывает меню команд
        │
        ├─→ Отправляет /daily
        │   └─→ Бот: "✅ Вы получили 10 токенов!"
        │       └─→ В базе данных: user.tokens += 10
        │
        ├─→ Отправляет /app
        │   └─→ Бот показывает кнопку [🚀 Open App]
        │       │
        │       └─→ Пользователь нажимает кнопку
        │           │
        │           └─→ Открывается Mini App (веб-страница)
        │               │
        │               ├─→ Видит свой Dashboard:
        │               │   - 150 KIK Tokens
        │               │   - 5 NFT картинок
        │               │   - Level 3
        │               │
        │               ├─→ Нажимает [Claim Daily]
        │               │   └─→ GET /api/claim
        │               │       └─→ Сервер: +10 токенов
        │               │
        │               ├─→ Переходит в Collection
        │               │   └─→ Видит свои NFT картинки
        │               │
        │               └─→ Переходит в Leaderboard
        │                   └─→ Видит топ пользователей
        │
        ├─→ Отправляет фото
        │   └─→ Бот: "Фото прикреплено к токену #42"
        │       └─→ Создается NFT с этим фото
        │
        └─→ Отправляет /invite
            └─→ Бот: "Ваша реферальная ссылка: t.me/bot?start=ref_12345"
                └─→ Приглашает друга → +бонусы
```

### 💎 User Journey - Wallet App (Web3 функционал)

```
🌐 Пользователь → Открывает browser
    │
    └─→ Переходит на https://kik-wallet-app.vercel.app
        │
        ├─→ Видит: [Connect Wallet]
        │   │
        │   └─→ Нажимает кнопку
        │       │
        │       └─→ MetaMask popup: "Подключить кошелек?"
        │           │
        │           └─→ Подключается
        │               │
        │               └─→ Видит Dashboard:
        │                   - Баланс: 1,000 KIK
        │                   - В $: ~$10.50
        │                   - Level 5
        │
        ├─→ Переходит в Transfer
        │   └─→ Вводит адрес друга
        │       └─→ Вводит 50 KIK
        │           └─→ [Send]
        │               └─→ MetaMask: подтвердить транзакцию
        │                   └─→ Токены отправлены ✅
        │
        └─→ Переходит в Rental
            └─→ Видит токены друзей для аренды
                └─→ Арендует 100 KIK на 24 часа
                    └─→ Может использовать временно
```

---

## 6️⃣ ЧТО УЖЕ ГОТОВО

### ✅ Готовые компоненты telegram-bot/

```
✅ telegram-bot/
   ✅ api/webhook.js           - Обработчик Telegram webhook
   ✅ api/health.js            - Health check
   ✅ api/claim.js             - API получения токенов
   ✅ api/user/[userId].js     - API данных пользователя
   ✅ public/index.html        - Mini App интерфейс
   ✅ public/css/app.css       - Стили
   ✅ public/js/app.js         - JavaScript логика
   ✅ src/commands/            - Команды бота
   ✅ src/services/            - Бизнес-логика
   ✅ src/locales/             - 28 языков
   ✅ vercel.json              - Конфигурация Vercel
   ✅ .env.production.example  - Пример переменных
   ✅ package.json             - Зависимости
```

### ✅ Готовые компоненты wallet-app/

```
✅ wallet-app/
   ✅ app/page.tsx             - Главная страница
   ✅ components/              - React компоненты
   ✅ lib/contracts.ts         - Конфигурация контрактов
   ✅ vercel.json              - Конфигурация Vercel
   ✅ .env.local               - Локальные переменные
   ✅ .env.production.example  - Пример для продакшена
   ✅ package.json             - Зависимости
```

### ✅ Готовые Smart контракты

```
✅ contracts/KIKTokenV4.sol
✅ Развернут на Polygon Amoy Testnet
✅ Адрес: 0x636C84f54cE96dfb4AE8B0D7c1420170bF8c22b7
```

### ✅ Готовая документация

```
✅ TELEGRAM_BOT_VERCEL_GUIDE.md  - Полная инструкция деплоя бота
✅ VERCEL_DEPLOYMENT_GUIDE.md    - Инструкция для wallet-app
✅ VERCEL_QUICK_START.md         - Быстрый старт
```

---

## 7️⃣ ЧТО НУЖНО СДЕЛАТЬ ДАЛЬШЕ

### 🎯 Чек-лист для деплоя

#### ☐ Шаг 1: Подготовить Telegram бота

1. Открыть Telegram → найти @BotFather
2. Создать бота: `/newbot`
3. Получить `BOT_TOKEN`
4. Настроить команды: `/setcommands`

#### ☐ Шаг 2: Развернуть telegram-bot на Vercel

1. Открыть https://vercel.com
2. Import project: `kik-token` репозиторий
3. **Root Directory:** `telegram-bot`
4. Добавить переменные окружения
5. Deploy

#### ☐ Шаг 3: Настроить Webhook

1. После деплоя получить URL: `https://your-bot.vercel.app`
2. Установить webhook:
   ```
   https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-bot.vercel.app/api/webhook
   ```

#### ☐ Шаг 4: Протестировать бота

1. Открыть бота в Telegram
2. Отправить `/start`
3. Отправить `/app` → должно открыться Mini App

#### ☐ Шаг 5: Развернуть wallet-app на Vercel (опционально)

1. Import project: тот же `kik-token` репозиторий
2. **Root Directory:** `wallet-app`
3. Добавить переменные окружения
4. Deploy

#### ☐ Шаг 6: Протестировать wallet-app

1. Открыть URL wallet-app
2. Подключить MetaMask
3. Проверить баланс KIK токенов

---

## 8️⃣ ДОПОЛНИТЕЛЬНЫЕ API ENDPOINTS (ЧТО ОСТАЛОСЬ СОЗДАТЬ)

### 📝 Нужно добавить еще API функции (опционально)

```
telegram-bot/api/
├── leaderboard.js        - GET /api/leaderboard
├── friends.js            - GET /api/friends?userId=X
├── collection.js         - GET /api/collection/:userId
├── generate-ai.js        - POST /api/generate-ai
├── upload-photo.js       - POST /api/upload-photo
└── make-public.js        - POST /api/make-public
```

**Эти endpoints можно создать позже**, основные уже готовы.

---

## 9️⃣ FAQ - Частые вопросы

### ❓ Нужно ли деплоить оба проекта?

**Нет, достаточно telegram-bot/**

- `telegram-bot/` - это **главный проект** с ботом и Mini App
- `wallet-app/` - это **дополнительное** Web3 приложение (можно деплоить отдельно потом)

### ❓ Где хранятся данные пользователей?

Сейчас используется **in-memory база данных** (src/database/db.js).  
Для продакшена рекомендуется подключить PostgreSQL или MongoDB.

### ❓ Как работает мультиязычность?

- В `src/locales/` есть 28 файлов с переводами
- Бот автоматически определяет язык пользователя
- Команда `/language` позволяет сменить язык

### ❓ Где NFT картинки хранятся?

Пока картинки генерируются как placeholder URL.  
Для продакшена нужно интегрировать:
- IPFS для хранения
- Pinata / NFT.Storage
- Или S3 / Cloudinary

### ❓ Как работает реферальная система?

1. Пользователь получает реферальную ссылку: `t.me/bot?start=ref_12345`
2. Друг переходит по ссылке → бот регистрирует реферала
3. Оба получают бонусные токены

### ❓ Связаны ли telegram-bot и wallet-app?

**Нет напрямую**, это отдельные приложения:
- `telegram-bot` - для Telegram пользователей (не нужен MetaMask)
- `wallet-app` - для Web3 пользователей (нужен MetaMask)

Но оба работают с одним смарт-контрактом KIK Token.

---

## 🎉 ЗАКЛЮЧЕНИЕ

### Что у вас есть:

1. ✅ **telegram-bot/** - Полностью готов к деплою на Vercel
2. ✅ **wallet-app/** - Полностью готов к деплою на Vercel
3. ✅ **Smart контракт** - Развернут на Polygon Amoy
4. ✅ **Документация** - Подробные инструкции

### Следующий шаг:

Откройте `TELEGRAM_BOT_VERCEL_GUIDE.md` и следуйте инструкциям по деплою!

### Полезные ссылки:

- 📖 Инструкция деплоя: `TELEGRAM_BOT_VERCEL_GUIDE.md`
- 🔗 GitHub: https://github.com/cashclouds/kik-token
- 🌐 Vercel: https://vercel.com/dashboard
- 💎 Контракт: https://amoy.polygonscan.com/address/0x636C84f54cE96dfb4AE8B0D7c1420170bF8c22b7

---

**Дата создания:** 12.12.2025  
**Версия:** 1.0  
**Статус:** ✅ Готов к деплою
